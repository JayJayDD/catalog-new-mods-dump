Folder contains assets for every mod. 
The ones that are used by the catalog are the thumbnail (thumb.png) and one sheet of animation (anims/*.png)

//Why *.cfg files are here?
Mostly for the preservation purposes. 
Would have been neat to use these directly to fetch data for the mods but not every mod has meta.cfg file, so it's not worth the hassle.